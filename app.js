var file
var stringParse = require("stringParse");
var fs = require('fs');
var printerState = {
    extruderTemp;
    bedTemp;
    x;
    y;
    z;
    xMin;
    xMax;
    yMin;
    yMax;
    zMin;
    zMax;
    extruderSteps;
}
fs.readFile("/gode/"+file+".stl","utf8")

while (!eof(file)) {
    var code = f.readline()
    if (code = G1) {}
    if (code = G28) {}
    if (code = G92) {}
    if (code = M84) {}
    if (code = M107) {}
}

function G1(code) {
    if (code = X) {
        int X = code;
    }
    if (code = Y) {
        int Y = code;
    }
    if (code = Z) {
        int Z = code
    }
    if (code = E) {
        int E = codez
    }
    if (code = F) {}
}

function G92(code) {
    if (code = X) {}
    if (code = Y) {}
    if (code = Z) {}
    if (code = E) {}
}

function M106(code) {
    if (code = S) {}
    if (code = P1) {

    }
}
